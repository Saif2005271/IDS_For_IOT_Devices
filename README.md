# IDS_For_IOT_Devices
This project appears to be a Python-based Intrusion Detection System (IDS) designed to monitor and secure IoT (Internet of Things) devices. The project is structured with several Python scripts to handle threat detection, dashboard visualization, and data extraction.
Project Files and Components
Based on the fetched contents of the Submission.rar archive, the project includes the following key components:

IDS/IDS.py
This is likely the main engine of the Intrusion Detection System. It would handle packet sniffing or log analysis to identify malicious activities or policy violations within the network.

IDS/Check_IP.py
A Python script used for IP verification. It likely checks incoming or outgoing IP addresses against databases of known malicious IPs, blacklists, or specific IoCs (Indicators of Compromise).

IDS/get_IoCs.py
This script retrieves "Indicators of Compromise" (IoCs). It likely connects to a threat intelligence feed or an external API to download current lists of malicious IPs, domains, or file hashes to update the IDS's detection capabilities.

IDS/dashboard.py
A script intended to serve as the user interface or visualization layer of the project. It likely processes the alerts generated by the IDS and displays them in a readable format for monitoring.

IDS/Conf.conf
A configuration file that contains the core settings for the IDS. The snippet reveals it is configured to monitor a specific network interface:

nic = Wi-Fi

mode = 1

IDS/.env
An environment variables file containing sensitive credentials required for the application to interact with external services. It contains a specific API_KEY (e.g., API_KEY="411cb1e56..."), which is likely used by get_IoCs.py to authenticate with a threat intelligence API.

IDS/templates/index.html
An HTML template file, indicating that the dashboard is web-based (likely using a Python web framework like Flask or FastAPI) and rendered through this front-end file.

IDS For IOT Devices.pptx
A PowerPoint presentation file that likely serves as the project's documentation, slide deck for a defense/pitch, or an overview explaining how the Intrusion Detection System is specifically tailored for IoT devices.

__pycache__ Directory
Contains compiled Python files (.pyc) for IDS.py, Check_IP.py, and get_IoCs.py, showing that the code has been executed.
